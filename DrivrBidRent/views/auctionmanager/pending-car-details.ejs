<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DRIVBIDRENT - Car Details</title>
    <style>
<<<<<<< Updated upstream
<<<<<<< Updated upstream
      @import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap");
=======
      @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap');
>>>>>>> Stashed changes
=======
      @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap');
>>>>>>> Stashed changes

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
<<<<<<< Updated upstream
<<<<<<< Updated upstream
        font-family: "Montserrat", sans-serif;
=======
        font-family: 'Montserrat', sans-serif;
>>>>>>> Stashed changes
=======
        font-family: 'Montserrat', sans-serif;
>>>>>>> Stashed changes
      }

      body {
        background-color: #ffffff;
      }

      .header {
        background-color: #ff6b00;
        color: white;
        padding: 15px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: 80px;
      }

      .logo {
        font-size: 24px;
        font-weight: bold;
      }

      .logo a {
        color: white;
        text-decoration: none;
      }

      .nav-links {
        display: flex;
      }

      .nav-links a {
        color: white;
        text-decoration: none;
        margin-left: 20px;
        font-weight: bold;
        transition: opacity 0.3s ease;
      }

      .nav-links a:hover {
        opacity: 0.8;
      }

      .car-details-container {
        max-width: 1000px;
        margin: 120px auto 20px;
        padding: 30px 20px;
      }

      .car-image-container {
        width: 100%;
        height: 300px;
        overflow: hidden;
        border-radius: 10px;
        margin-bottom: 20px;
      }

      .car-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .car-name {
        font-size: 28px;
        font-weight: bold;
        color: #333;
        margin-bottom: 10px;
      }

      .car-info {
        font-size: 18px;
        color: #666;
        margin-bottom: 10px;
      }

      .car-info strong {
        color: #333;
      }

      .rating-section {
        margin-top: 20px;
      }

      .rating-section h3 {
        font-size: 22px;
        color: #333;
        margin-bottom: 10px;
      }

      .rating-item {
        margin-bottom: 15px;
      }

      .rating-item p {
        font-size: 16px;
        color: #666;
        margin-bottom: 5px;
      }

      .rating-item textarea {
        width: 100%;
        padding: 10px;
        border-radius: 5px;
        border: 1px solid #ddd;
        resize: vertical;
      }

      .action-buttons {
        display: flex;
        gap: 10px;
        margin-top: 20px;
      }

      .approve-btn,
      .decline-btn {
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: opacity 0.3s ease;
      }

      .approve-btn {
        background-color: #4caf50;
        color: white;
      }

      .approve-btn:disabled {
        background-color: #cccccc;
        cursor: not-allowed;
        opacity: 0.6;
      }

      .decline-btn {
        background-color: #f44336;
        color: white;
      }

      .approve-btn:hover:not(:disabled),
      .decline-btn:hover {
        opacity: 0.8;
      }

      .status {
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        color: white;
        margin-top: 20px;
        display: inline-block;
      }

      .approved {
        background-color: #4caf50;
      }

      .declined {
        background-color: #f44336;
<<<<<<< Updated upstream
<<<<<<< Updated upstream
=======
=======
>>>>>>> Stashed changes
      }

      .error-message {
        color: #f44336;
        font-size: 16px;
        margin-bottom: 20px;
      }

      .no-review-message {
        color: #f44336;
        font-size: 16px;
        margin-top: 10px;
<<<<<<< Updated upstream
>>>>>>> Stashed changes
=======
>>>>>>> Stashed changes
      }
    </style>
  </head>
  <body>
<<<<<<< Updated upstream
<<<<<<< Updated upstream
    <%- include('partials/navbar') %>

    <!-- Car Details Container -->
    <div class="car-details-container">
      <div class="car-image-container">
        <img
          src="/images/tata_toofan.png"
          alt="Tata Toofan"
          class="car-image"
        />
      </div>
      <div class="car-name">Tata Toofan</div>
      <div class="car-info"><strong>Mileage:</strong> 35,000 km</div>
      <div class="car-info"><strong>Condition:</strong> Pending</div>
=======
    <!-- Header -->
    <div class="header">
      <div class="logo"><a href="/auctionmanager/home1">DRIVBIDRENT</a></div>
      <div class="nav-links">
        <a href="/auctionmanager/requests">REQUESTS</a>
        <a href="/auctionmanager/pending">PENDINGCARS</a>
        <a href="/auctionmanager/approvedcars">APPROVEDCARS</a>
      </div>
=======
    <!-- Header -->
    <div class="header">
      <div class="logo"><a href="/auctionmanager/home1">DRIVBIDRENT</a></div>
      <div class="nav-links">
        <a href="/auctionmanager/requests">REQUESTS</a>
        <a href="/auctionmanager/pending">PENDINGCARS</a>
        <a href="/auctionmanager/approvedcars">APPROVEDCARS</a>
      </div>
    </div>

    <!-- Car Details Container -->
    <div class="car-details-container">
      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } else if (!car) { %>
        <div class="error-message">Car not found</div>
      <% } else { %>
        <div class="car-image-container">
          <img src="<%= car.vehicleImage %>" alt="<%= car.vehicleName %>" class="car-image" />
        </div>
        <div class="car-name"><%= car.vehicleName %></div>
        <div class="car-info"><strong>Year:</strong> <%= car.year %></div>
        <div class="car-info"><strong>Mileage:</strong> <%= car.mileage %> km</div>
        <div class="car-info"><strong>Condition:</strong> <%= car.condition %></div>
        <div class="car-info"><strong>Fuel Type:</strong> <%= car.fuelType %></div>
        <div class="car-info"><strong>Transmission:</strong> <%= car.transmission %></div>
        <div class="car-info"><strong>Starting Bid:</strong> $<%= car.startingBid %></div>
        <div class="car-info"><strong>Auction Date:</strong> <%= new Date(car.auctionDate).toLocaleDateString() %></div>
        <div class="car-info"><strong>Status:</strong> <%= car.status %></div>

        <!-- Mechanic's Ratings and Comments -->
        <div class="rating-section">
          <h3>Mechanic's Ratings & Comments</h3>
          <% if (car.mechanicReview && car.mechanicReview.mechanicalCondition && car.mechanicReview.bodyCondition && car.mechanicReview.submittedAt) { %>
            <div class="rating-item">
              <p><strong>Mechanical Condition:</strong> <%= '★'.repeat(car.mechanicReview.conditionRating || 0) %>☆<%= '☆'.repeat(5 - (car.mechanicReview.conditionRating || 0)) %></p>
              <textarea readonly><%= car.mechanicReview.mechanicalCondition %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Body Condition:</strong> <%= '★'.repeat(car.mechanicReview.conditionRating || 0) %>☆<%= '☆'.repeat(5 - (car.mechanicReview.conditionRating || 0)) %></p>
              <textarea readonly><%= car.mechanicReview.bodyCondition %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Recommendations:</strong></p>
              <textarea readonly><%= car.mechanicReview.recommendations || 'No recommendations provided.' %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Submitted At:</strong> <%= new Date(car.mechanicReview.submittedAt).toLocaleString() %></p>
            </div>
            <div class="rating-item">
              <p><strong>Mechanic:</strong> <%= car.assignedMechanic ? car.assignedMechanic.firstName + ' ' + car.assignedMechanic.lastName : 'Unknown' %></p>
            </div>
          <% } else { %>
            <p class="no-review-message">No mechanic review available yet. Approval is disabled until a review is submitted.</p>
          <% } %>
        </div>

        <!-- Action Buttons -->
        <div class="action-buttons" id="action-buttons">
          <button class="approve-btn" id="approve-btn" 
            <%= (!car.mechanicReview || !car.mechanicReview.mechanicalCondition || !car.mechanicReview.bodyCondition || !car.mechanicReview.submittedAt) ? 'disabled' : '' %> 
            onclick="updateStatus('approved')">Approve</button>
          <button class="decline-btn" onclick="updateStatus('rejected')">Decline</button>
        </div>

        <!-- Status (Approved/Declined) -->
        <div id="status" class="status" style="display: none"></div>
      <% } %>
>>>>>>> Stashed changes
    </div>

    <!-- Car Details Container -->
    <div class="car-details-container">
      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } else if (!car) { %>
        <div class="error-message">Car not found</div>
      <% } else { %>
        <div class="car-image-container">
          <img src="<%= car.vehicleImage %>" alt="<%= car.vehicleName %>" class="car-image" />
        </div>
        <div class="car-name"><%= car.vehicleName %></div>
        <div class="car-info"><strong>Year:</strong> <%= car.year %></div>
        <div class="car-info"><strong>Mileage:</strong> <%= car.mileage %> km</div>
        <div class="car-info"><strong>Condition:</strong> <%= car.condition %></div>
        <div class="car-info"><strong>Fuel Type:</strong> <%= car.fuelType %></div>
        <div class="car-info"><strong>Transmission:</strong> <%= car.transmission %></div>
        <div class="car-info"><strong>Starting Bid:</strong> $<%= car.startingBid %></div>
        <div class="car-info"><strong>Auction Date:</strong> <%= new Date(car.auctionDate).toLocaleDateString() %></div>
        <div class="car-info"><strong>Status:</strong> <%= car.status %></div>
>>>>>>> Stashed changes

        <!-- Mechanic's Ratings and Comments -->
        <div class="rating-section">
          <h3>Mechanic's Ratings & Comments</h3>
          <% if (car.mechanicReview && car.mechanicReview.mechanicalCondition && car.mechanicReview.bodyCondition && car.mechanicReview.submittedAt) { %>
            <div class="rating-item">
              <p><strong>Mechanical Condition:</strong> <%= '★'.repeat(car.mechanicReview.conditionRating || 0) %>☆<%= '☆'.repeat(5 - (car.mechanicReview.conditionRating || 0)) %></p>
              <textarea readonly><%= car.mechanicReview.mechanicalCondition %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Body Condition:</strong> <%= '★'.repeat(car.mechanicReview.conditionRating || 0) %>☆<%= '☆'.repeat(5 - (car.mechanicReview.conditionRating || 0)) %></p>
              <textarea readonly><%= car.mechanicReview.bodyCondition %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Recommendations:</strong></p>
              <textarea readonly><%= car.mechanicReview.recommendations || 'No recommendations provided.' %></textarea>
            </div>
            <div class="rating-item">
              <p><strong>Submitted At:</strong> <%= new Date(car.mechanicReview.submittedAt).toLocaleString() %></p>
            </div>
            <div class="rating-item">
              <p><strong>Mechanic:</strong> <%= car.assignedMechanic ? car.assignedMechanic.firstName + ' ' + car.assignedMechanic.lastName : 'Unknown' %></p>
            </div>
          <% } else { %>
            <p class="no-review-message">No mechanic review available yet. Approval is disabled until a review is submitted.</p>
          <% } %>
        </div>

        <!-- Action Buttons -->
        <div class="action-buttons" id="action-buttons">
          <button class="approve-btn" id="approve-btn" 
            <%= (!car.mechanicReview || !car.mechanicReview.mechanicalCondition || !car.mechanicReview.bodyCondition || !car.mechanicReview.submittedAt) ? 'disabled' : '' %> 
            onclick="updateStatus('approved')">Approve</button>
          <button class="decline-btn" onclick="updateStatus('rejected')">Decline</button>
        </div>

        <!-- Status (Approved/Declined) -->
        <div id="status" class="status" style="display: none"></div>
      <% } %>
    </div>

    <%- include('partials/footer') %>

    <script>
<<<<<<< Updated upstream
<<<<<<< Updated upstream
      function approveCar() {
        const buttons = document.getElementById("action-buttons");
        const status = document.getElementById("status");

        buttons.style.display = "none";
        status.textContent = "Approved";
        status.className = "approved";
        status.style.display = "block";
      }

      function declineCar() {
        const buttons = document.getElementById("action-buttons");
        const status = document.getElementById("status");

        buttons.style.display = "none";
        status.textContent = "Declined";
        status.className = "declined";
        status.style.display = "block";
=======
      async function updateStatus(status) {
        const carId = '<%= car ? car._id : "" %>';
        const buttons = document.getElementById('action-buttons');
        const statusDiv = document.getElementById('status');

        // Prevent approval client-side if mechanic review is incomplete
        if (status === 'approved') {
          const hasReview = <%= car && car.mechanicReview && car.mechanicReview.mechanicalCondition && car.mechanicReview.bodyCondition && car.mechanicReview.submittedAt ? 'true' : 'false' %>;
          if (!hasReview) {
            alert('Cannot approve car without a complete mechanic review');
            return;
          }
        }

        try {
          const response = await fetch(`/auctionmanager/update-status/${carId}`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ status }),
          });

=======
      async function updateStatus(status) {
        const carId = '<%= car ? car._id : "" %>';
        const buttons = document.getElementById('action-buttons');
        const statusDiv = document.getElementById('status');

        // Prevent approval client-side if mechanic review is incomplete
        if (status === 'approved') {
          const hasReview = <%= car && car.mechanicReview && car.mechanicReview.mechanicalCondition && car.mechanicReview.bodyCondition && car.mechanicReview.submittedAt ? 'true' : 'false' %>;
          if (!hasReview) {
            alert('Cannot approve car without a complete mechanic review');
            return;
          }
        }

        try {
          const response = await fetch(`/auctionmanager/update-status/${carId}`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ status }),
          });

>>>>>>> Stashed changes
          const result = await response.json();

          if (result.success) {
            buttons.style.display = 'none';
            statusDiv.textContent = status.charAt(0).toUpperCase() + status.slice(1);
            statusDiv.className = `status ${status === 'approved' ? 'approved' : 'declined'}`;
            statusDiv.style.display = 'block';
          } else {
            alert(result.error || 'Mechanic has not yet gave his review');
          }
        } catch (err) {
          console.error('Error updating status:', err);
          alert('Failed to update status');
        }
<<<<<<< Updated upstream
>>>>>>> Stashed changes
=======
>>>>>>> Stashed changes
      }
    </script>
  </body>
</html>